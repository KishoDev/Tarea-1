# Tarea-2
---

Recomendaciones a la hora de compilar y probar:
  ES IMPORTANTE tener "problema1.cpp" y "lista.hpp" dentro de la misma carpeta a la hora de compilar y ejecutar el programa. 
  Tal programa no funcionará sin el header recién indicado.

Formas de compilación utilizadas:
 Para los casos de pruebas se utilizó el compilador GCC en su versión 13.1.1 con el comando "g++ archivo.cpp -o output -Wall".
 
 Para el problema 2 seguir las siguientes instrucciones
  Para compilar el programa se necesitan dos archivos: 'Tarea2_ejer2.cpp' y 'TDA de ABB.hpp'. Ambos archivos deben estar en la 
  misma carpeta al momento de ser compilado
  
  Formas de compilación utilizada:
    Para compilar este archivo se utilizo una terminal de ubuntu (version:22.04), y se utilizo el compilador GCC en su
    version 11.3.0 con el comando 'g++ archivo.cpp -o salida'
